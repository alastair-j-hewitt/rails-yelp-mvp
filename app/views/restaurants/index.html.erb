<h1>Restaurant#index</h1>

<p>
  <% @restaurants.each do |restaurant| %>
    <p>
      <%= restaurant.name %>
    </p>
  <% end %>

  <button type="button" class="btn btn-primary">
      <% @restaurants.create %>add restaurant
    </button>
</p>
